# ใบสูตรสำเร็จรูป ปฏิบัติการที่ 13

**การวิเคราะห์และนำเสนอข้อมูลทางพันธุศาสตร์ด้วยสเปรดชีต** · พิมพ์แผ่นนี้วางไว้ข้างแป้นพิมพ์ระหว่างทำงาน

---

## ส่วนที่ 1 ฟังก์ชันใหม่ 5 ตัวของคาบนี้

| ฟังก์ชัน | รูปแบบ | ได้อะไร | ตัวอย่าง |
|---|---|---|---|
| `STDEV` | `STDEV(ช่วงข้อมูล)` | ส่วนเบี่ยงเบนมาตรฐานแบบตัวอย่าง | `=STDEV(D4:D323)` |
| `SQRT` | `SQRT(ตัวเลข)` | รากที่สอง | `=SQRT(320)` |
| `MEDIAN` | `MEDIAN(ช่วงข้อมูล)` | มัธยฐาน ค่าที่อยู่ตรงกลาง | `=MEDIAN(D4:D323)` |
| `CHITEST` | `CHITEST(ช่วง O, ช่วง E)` | **ค่า p** โดยโปรแกรมคิด df ให้เอง | `=CHITEST(C6:C8,E6:E8)` |
| `CHIDIST` | `CHIDIST(ค่าไคสแควร์, df)` | ค่า p โดยเราระบุ df เอง | `=CHIDIST(B21,1)` |

> **ข้อควรระวังข้อที่ 1** `CHITEST` และ `CHIDIST` **คืนค่า p ไม่ใช่ค่าไคสแควร์** ในสเปรดชีตไม่มีฟังก์ชันใดที่รับ O กับ E แล้วคืนค่าไคสแควร์ให้ ค่าไคสแควร์ต้องคำนวณเองด้วย `(O-E)^2/E` แล้วรวมกันเสมอ
>
> **ข้อควรระวังข้อที่ 2** `CHITEST` คิด df จากขนาดของช่วงข้อมูลเสมอ จึงใช้ได้กับการทดสอบอัตราส่วนเมนเดลในแท็บ 04 เท่านั้น **ห้ามใช้กับการทดสอบฮาร์ดี-ไวน์เบิร์กในแท็บ 06** ให้ใช้ `CHIDIST` แทน

---

## ส่วนที่ 1.5 การตัดสินผล สองวิธีที่ต้องได้ข้อสรุปตรงกัน

| วิธี | ตัดสินอย่างไร | เครื่องหมาย |
|---|---|---|
| ค่าวิกฤต (ที่เคยเรียน) | ไคสแควร์ **มากกว่า** ค่าวิกฤต จึงปฏิเสธ H0 | มากกว่า |
| ค่า p (เรื่องใหม่) | ค่า p **น้อยกว่า** 0.05 จึงปฏิเสธ H0 | น้อยกว่า |

**จำให้แม่น ทิศทางสลับกัน** ไคสแควร์ยิ่งมากยิ่งขัดกับ H0 แต่ค่า p ยิ่งน้อยยิ่งขัดกับ H0

| ค่า p ที่ได้ | การตัดสิน |
|---|---|
| p มากกว่าหรือเท่ากับ 0.05 | ยอมรับ H0 ไม่แตกต่างอย่างมีนัยสำคัญ |
| p น้อยกว่า 0.05 | ปฏิเสธ H0 แตกต่างอย่างมีนัยสำคัญ |

**สมมติฐานหลักของสองการทดสอบในคาบนี้**

| แท็บ | H0 |
|---|---|
| 04 | ผลการทดลองเป็นไปตามกฎของเมนเดล คืออัตราส่วน 1:2:1 |
| 06 | ประชากรอยู่ในสมดุลฮาร์ดี-ไวน์เบิร์ก |

---

## ส่วนที่ 2 ฟังก์ชันที่ทบทวนจากปฏิบัติการที่ 12

| ฟังก์ชัน | รูปแบบ | ได้อะไร |
|---|---|---|
| `COUNT` | `COUNT(ช่วง)` | นับเฉพาะช่องที่เป็นตัวเลข |
| `COUNTA` | `COUNTA(ช่วง)` | นับทุกช่องที่ไม่ว่าง รวมข้อความ |
| `COUNTIF` | `COUNTIF(ช่วง, เงื่อนไข)` | นับตามเงื่อนไขเดียว |
| `COUNTIFS` | `COUNTIFS(ช่วง1, เงื่อนไข1, ช่วง2, เงื่อนไข2)` | นับเมื่อเข้าทุกเงื่อนไขพร้อมกัน |
| `SUM` | `SUM(ช่วง)` | ผลรวม |
| `AVERAGE` | `AVERAGE(ช่วง)` | ค่าเฉลี่ย |
| `MIN` `MAX` | `MIN(ช่วง)` | ค่าต่ำสุด ค่าสูงสุด |
| `ROUND` | `ROUND(ตัวเลข, ตำแหน่งทศนิยม)` | ปัดเศษ |
| `IF` | `IF(เงื่อนไข, ถ้าจริง, ถ้าเท็จ)` | เลือกผลลัพธ์ตามเงื่อนไข |
| `AND` | `AND(เงื่อนไข1, เงื่อนไข2)` | จริงเมื่อจริงทุกข้อ |
| `VLOOKUP` | `VLOOKUP(ค่าที่ค้น, ตาราง, คอลัมน์ที่เอา, FALSE)` | ค้นค่าในตาราง |
| `&` | `"ข้อความ"&ช่อง` | ต่อข้อความเข้ากับค่าในช่อง |

---

## ส่วนที่ 3 สูตรเรียงตามลำดับการทำงาน

### M1 สถิติพรรณนา แท็บ `02_สถิติพรรณนา`

| ต้องการ | สูตร |
|---|---|
| จำนวนต้นทั้งหมด | `=COUNTA('01_ข้อมูล_F2'!$A$4:$A$xxx)` |
| ค่าเฉลี่ย | `=AVERAGE('01_ข้อมูล_F2'!$D$4:$D$xxx)` |
| มัธยฐาน | `=MEDIAN(ช่วงเดียวกัน)` |
| พิสัย | `=B10-B9` คือ ค่าสูงสุด ลบ ค่าต่ำสุด |
| ส่วนเบี่ยงเบนมาตรฐาน | `=STDEV(ช่วงเดียวกัน)` |
| ความคลาดเคลื่อนมาตรฐาน | `=B12/SQRT(B6)` คือ SD หารด้วยรากที่สองของ n |
| สัมประสิทธิ์การแปรผัน | `=ROUND(B12/B7*100,2)` |
| แยกความสูงตามจีโนไทป์ | `=IF('01_ข้อมูล_F2'!$B4="CRCR",'01_ข้อมูล_F2'!$D4,"")` |

**จุดพลาดบ่อย** ช่อง SE ต้องหารด้วย `SQRT(n)` ไม่ใช่หารด้วย `n` และต้องอ้างถึงช่อง SD ที่แถว 12 ไม่ใช่ช่องพิสัยที่แถว 11

### M2 ตารางแจกแจงความถี่ แท็บ `03_การแจกแจงความถี่`

| ต้องการ | สูตร |
|---|---|
| ความถี่ของชั้น | `=COUNTIFS(ช่วงความสูง,">="&B5,ช่วงความสูง,"<"&C5)` |
| ความถี่สัมพัทธ์ | `=D5/$D$18*100` |

**จุดพลาดบ่อย** ขอบบนต้องใช้ `"<"` ไม่ใช่ `"<="` มิฉะนั้นต้นที่อยู่พอดีขอบจะถูกนับสองชั้น

### M3 ไคสแควร์อัตราส่วน 1:2:1 แท็บ `04_ไคสแควร์_เมนเดล`

| ต้องการ | สูตร |
|---|---|
| จำนวนที่นับได้ O | `=COUNTIF('01_ข้อมูล_F2'!$B$4:$B$xxx,A6)` |
| จำนวนที่คาดหมาย E | `=$C$9*D6` |
| ส่วนต่าง | `=C6-E6` |
| (O-E)² / E | `=(C6-E6)^2/E6` |
| ค่าไคสแควร์ | `=SUM(G6:G8)` |
| องศาอิสระ | `=COUNTA(A6:A8)-1` ได้ **2** |
| ค่าวิกฤต | `=VLOOKUP(C13,Lookup_ChiSquare!$A$4:$C$13,2,FALSE)` ได้ **5.991** |
| ค่า p | `=CHITEST(C6:C8,E6:E8)` |

### M4 ไคสแควร์ฮาร์ดี-ไวน์เบิร์ก แท็บ `06_ไคสแควร์_HWE`

| ต้องการ | สูตร |
|---|---|
| จำนวนอัลลีล C^R | `=2*'05_ข้อมูล_ประชากร'!C5+'05_ข้อมูล_ประชากร'!C6` |
| จำนวนอัลลีลทั้งหมด | `=2*B5` |
| ความถี่ p | `=B6/B8` |
| ความถี่ q | `=B7/B8` หรือ `=1-B9` |
| ความถี่คาดหมาย `CRCR` | `=$B$9^2` |
| ความถี่คาดหมาย `CRCW` | `=2*$B$9*$B$10` |
| ความถี่คาดหมาย `CWCW` | `=$B$10^2` |
| จำนวนที่คาดหมาย E | `=$B$5*D15` |
| (O-E)² / E | `=(C15-E15)^2/E15` |
| องศาอิสระ | พิมพ์ **1** ลงไปเอง  df = (3 genotype - 1) - (2 alleles - 1) |
| ค่าวิกฤต | `=VLOOKUP(B22,Lookup_ChiSquare!$A$4:$C$13,2,FALSE)` ได้ **3.841** |
| ค่า p ที่ถูกต้อง | `=CHIDIST(B21,B22)` |

### M5 กราฟ แท็บ `07_กราฟ`

| ต้องการ | สูตร |
|---|---|
| ดึงค่าเฉลี่ยข้ามแท็บ | `='02_สถิติพรรณนา'!D20` |
| ดึงค่า SE ข้ามแท็บ | `='02_สถิติพรรณนา'!F20` |

สร้างกราฟแท่งธรรมดา **ไม่ต้องใส่แถบความคลาดเคลื่อน** คอลัมน์ SE มีไว้ใช้เขียนประกอบในคำบรรยายภาพ

---

## ส่วนที่ 4 ตารางค่าวิกฤตไคสแควร์

| df | ค่าวิกฤตที่ 0.05 | ค่าวิกฤตที่ 0.01 | ใช้กับ |
|---|---|---|---|
| **1** | **3.841** | 6.635 | **แท็บ 06 ฮาร์ดี-ไวน์เบิร์ก  df = (3-1) - (2-1)** |
| **2** | **5.991** | 9.210 | **แท็บ 04 อัตราส่วน 1:2:1  df = 3 - 1** |
| 3 | 7.815 | 11.345 | |
| 4 | 9.488 | 13.277 | |
| 5 | 11.070 | 15.086 | |

**กฎการตัดสิน** ถ้าค่าไคสแควร์ที่คำนวณได้ **มากกว่า** ค่าวิกฤต ให้ปฏิเสธสมมติฐานหลัก

---

## ส่วนที่ 5 เหตุใด df ของสองการทดสอบจึงไม่เท่ากัน

| | แท็บ 04 อัตราส่วน 1:2:1 | แท็บ 06 ฮาร์ดี-ไวน์เบิร์ก |
|---|---|---|
| จำนวนกลุ่ม | 3 | 3 |
| ค่าที่คาดหมายมาจากไหน | ทฤษฎีล้วน คือ 0.25, 0.50, 0.25 | คำนวณจากข้อมูลเอง ต้องหา p ก่อน |
| ประมาณค่าจากข้อมูลกี่ตัว | 0 ตัว | 1 ตัว คือ p |
| **df** | **3 - 1 = 2** | **(3 - 1) - (2 - 1) = 1** |
| ใช้ฟังก์ชันใด | `CHITEST` ได้ | ต้องใช้ `CHIDIST` |

---

## ส่วนที่ 6 การตรวจสอบตนเองก่อนส่ง

| แท็บ | สิ่งที่ต้องตรวจ |
|---|---|
| 02 | ผลรวม n ของสามจีโนไทป์ ต้องเท่ากับ N และช่องตรวจสอบต้องขึ้นว่า ผ่าน |
| 03 | ผลรวมความถี่ ต้องเท่ากับ N และผลรวมความถี่สัมพัทธ์ ต้องได้ 100 |
| 04 | ผลรวม O ต้องเท่ากับผลรวม E และผลรวม O-E ต้องได้ศูนย์ และสองวิธีต้องสรุปตรงกัน |
| 06 | p + q ต้องได้ 1 และผลรวมความถี่คาดหมายต้องได้ 1 และผลรวม E ต้องเท่ากับ N |
| 07 | กราฟมีองค์ประกอบครบ 6 ข้อ และมีคำบรรยายภาพครบ 3 ส่วน |
| ทุกแท็บ | คลิกทีละช่องแล้วดูแถบสูตร ต้องขึ้นสูตร ไม่ใช่ตัวเลขที่พิมพ์ทับ |

---

## ส่วนที่ 7 รูปแบบการเขียนข้อสรุป

```text
ต้น F2 จำนวน ... ต้น แยกเป็นแดง ... ชมพู ... ขาว ... ซึ่งไม่แตกต่างจาก
อัตราส่วน 1:2:1 อย่างมีนัยสำคัญทางสถิติ (ไคสแควร์ = ..., df = 2, p = ...)

ประชากรที่สำรวจมีความถี่อัลลีล C^R เท่ากับ ... จำนวนเฮเทอโรไซกัสน้อยกว่าที่
คาดหมาย และแตกต่างจากสมดุลฮาร์ดี-ไวน์เบิร์กอย่างมีนัยสำคัญทางสถิติ
(ไคสแควร์ = ..., df = 1, p = ...) ซึ่งอาจเกิดจาก ...
```

**ต้องมีครบสี่อย่างเสมอ** ค่าสถิติทดสอบ · องศาอิสระ · ค่า p · การตีความเป็นภาษาคน
