# ใบสูตรสำเร็จรูป - ปฏิบัติการที่ 12

> **SC112502 ปฏิบัติการพันธุศาสตร์เบื้องต้น**
> การจัดการข้อมูลตารางและการสร้างกราฟด้วย Google Sheets
> ฟังก์ชันที่ต้องใช้ทั้งหมด 21 ตัวอยู่ในหน้านี้

ช่องแรกของทุกตอนในไฟล์มีสูตรเต็มอยู่แล้ว ให้คัดลอกไปวางแล้วดัดแปลง ฉบับกระดาษอยู่ที่ `06_ใบสูตรสำเร็จรูป/ใบสูตรสำเร็จรูป_12.pdf` พิมพ์แนวนอนหน้าเดียว วางไว้ข้างคอมพิวเตอร์ตลอดคาบ

## 6 อย่างที่ต้องจำ

1. เซลล์มีชื่อ เช่น B12
2. พิมพ์ = นำหน้าคือสูตร
3. อย่าพิมพ์ชื่อเซลล์เอง ให้คลิก
4. เครื่องหมาย : อ่านว่า ถึง
5. ดับเบิลคลิกมุมขวาล่างเพื่อเติมสูตรทั้งคอลัมน์
6. ตรึงด้วย $ โดยกด F4

## ฟังก์ชันทั้ง 21 ตัว

### ทำความสะอาดข้อมูล (M2)

ฟังก์ชันข้อความและตรรกะ ใช้ในแท็บ 03_Clean

| ฟังก์ชัน | รูปแบบที่ใช้จริง | อ่านว่า |
|---|---|---|
| `TRIM` | `=TRIM(D2)` | ตัดช่องว่างส่วนเกินหน้า-หลัง |
| `PROPER` | `=PROPER(TRIM(D2))` | ทำตัวแรกเป็นตัวใหญ่ ที่เหลือเป็นตัวเล็ก |
| `&` | `=G2&"-"&H2` | เชื่อมข้อความสองช่องเข้าด้วยกัน |
| `LEFT` | `=LEFT(A2,1)` | เอาตัวอักษรจากซ้ายมากี่ตัว |
| `MID` | `=MID(A2,3,3)` | เอาตัวอักษรจากตำแหน่งที่กำหนด |
| `LEN` | `=LEN(A2)` | นับจำนวนตัวอักษร |
| `SUBSTITUTE` | `=SUBSTITUTE(F2,"B","")` | แทนที่ข้อความด้วยอีกข้อความ |
| `COUNTIF` | `=COUNTIF($A$2:A2,A2)` | นับตามเงื่อนไขเดียว (ใช้ที่ M3 ด้วย) |
| `IF` | `=IF(N2=1,1,0)` | ถ้าเงื่อนไขจริงให้ค่าหนึ่ง ถ้าเท็จให้อีกค่าหนึ่ง |
| `AND` | `=IF(AND(C2>=20,C2<=400),1,0)` | ใช้เมื่อต้องเข้าเงื่อนไขสองข้อพร้อมกัน |

### สรุปด้วยสถิติพื้นฐาน (M3)

ใช้ในแท็บ 04_Summary

| ฟังก์ชัน | รูปแบบที่ใช้จริง | อ่านว่า |
|---|---|---|
| `COUNTA` | `=COUNTA(A2:A324)` | นับทุกช่องที่ไม่ว่าง (รวมข้อความ) |
| `COUNT` | `=COUNT(C2:C324)` | นับเฉพาะช่องที่เป็นตัวเลข |
| `MIN` | `=MIN(C2:C324)` | ค่าต่ำสุด - ใช้ตรวจข้อมูลผิดปกติ |
| `MAX` | `=MAX(C2:C324)` | ค่าสูงสุด - ใช้ตรวจข้อมูลผิดปกติ |
| `SUM` | `=SUM(T2:T324)` | ผลรวม |
| `AVERAGE` | `=AVERAGE(T2:T324)` | ค่าเฉลี่ย (ข้ามช่องว่างให้เอง) |
| `ROUND` | `=ROUND(B14,1)` | ปัดทศนิยมตามจำนวนตำแหน่งที่กำหนด |

### นับตามเงื่อนไขและค้นหา (M4)

ใช้ในแท็บ 05_Ratio

| ฟังก์ชัน | รูปแบบที่ใช้จริง | อ่านว่า |
|---|---|---|
| `COUNTIFS` | `=COUNTIFS(สี,"Red",ทรง,"Round",ใช้,1)` | นับเมื่อเข้าหลายเงื่อนไขพร้อมกัน |
| `SUMIFS` | `=SUMIFS(น้ำหนัก,สี,"Red",ใช้,1)` | บวกเฉพาะแถวที่เข้าเงื่อนไข |
| `VLOOKUP` | `=VLOOKUP("Red-Round",ตาราง,3,FALSE)` | ค้นค่าในตารางแล้วเอาคอลัมน์ที่ระบุ |
| `IFERROR` | `=IFERROR(สูตร,"ไม่พบ")` | ถ้าสูตรให้ค่าผิดพลาด ให้แสดงข้อความแทน |

## ตารางรวม เรียงตามตัวอักษร

| ฟังก์ชัน | รูปแบบ | อ่านว่า | ใช้ที่ |
|---|---|---|---|
| `&` | `=G2&"-"&H2` | เชื่อมข้อความสองช่องเข้าด้วยกัน | M2 |
| `AND` | `=IF(AND(C2>=20,C2<=400),1,0)` | ใช้เมื่อต้องเข้าเงื่อนไขสองข้อพร้อมกัน | M2 |
| `AVERAGE` | `=AVERAGE(T2:T324)` | ค่าเฉลี่ย (ข้ามช่องว่างให้เอง) | M3 |
| `COUNT` | `=COUNT(C2:C324)` | นับเฉพาะช่องที่เป็นตัวเลข | M3 |
| `COUNTA` | `=COUNTA(A2:A324)` | นับทุกช่องที่ไม่ว่าง (รวมข้อความ) | M3 |
| `COUNTIF` | `=COUNTIF($A$2:A2,A2)` | นับตามเงื่อนไขเดียว | M2, M3 |
| `COUNTIFS` | `=COUNTIFS(สี,"Red",ทรง,"Round",ใช้,1)` | นับเมื่อเข้าหลายเงื่อนไขพร้อมกัน | M4 |
| `IF` | `=IF(N2=1,1,0)` | ถ้าเงื่อนไขจริงให้ค่าหนึ่ง ถ้าเท็จให้อีกค่าหนึ่ง | M2 |
| `IFERROR` | `=IFERROR(สูตร,"ไม่พบ")` | ถ้าสูตรให้ค่าผิดพลาด ให้แสดงข้อความแทน | M4 |
| `LEFT` | `=LEFT(A2,1)` | เอาตัวอักษรจากซ้ายมากี่ตัว | M2 |
| `LEN` | `=LEN(A2)` | นับจำนวนตัวอักษร | M2 |
| `MAX` | `=MAX(C2:C324)` | ค่าสูงสุด - ใช้ตรวจข้อมูลผิดปกติ | M3 |
| `MID` | `=MID(A2,3,3)` | เอาตัวอักษรจากตำแหน่งที่กำหนด | M2 |
| `MIN` | `=MIN(C2:C324)` | ค่าต่ำสุด - ใช้ตรวจข้อมูลผิดปกติ | M3 |
| `PROPER` | `=PROPER(TRIM(D2))` | ทำตัวแรกเป็นตัวใหญ่ ที่เหลือเป็นตัวเล็ก | M2 |
| `ROUND` | `=ROUND(B14,1)` | ปัดทศนิยมตามจำนวนตำแหน่งที่กำหนด | M3 |
| `SUBSTITUTE` | `=SUBSTITUTE(F2,"B","")` | แทนที่ข้อความด้วยอีกข้อความ | M2 |
| `SUM` | `=SUM(T2:T324)` | ผลรวม | M3 |
| `SUMIFS` | `=SUMIFS(น้ำหนัก,สี,"Red",ใช้,1)` | บวกเฉพาะแถวที่เข้าเงื่อนไข | M4 |
| `TRIM` | `=TRIM(D2)` | ตัดช่องว่างส่วนเกินหน้า-หลัง | M2 |
| `VLOOKUP` | `=VLOOKUP("Red-Round",ตาราง,3,FALSE)` | ค้นค่าในตารางแล้วเอาคอลัมน์ที่ระบุ | M4 |

## ถ้าขึ้นค่าผิดพลาด

| สิ่งที่เห็น | แปลว่าอะไร |
|---|---|
| `#N/A` | VLOOKUP ค้นไม่เจอ ตรวจว่าสะกดตรงและใส่ FALSE แล้วหรือยัง |
| `#VALUE!` | ใส่ข้อความในที่ที่ต้องเป็นตัวเลข |
| `#REF!` | อ้างถึงช่องที่ถูกลบ |
| `#DIV/0!` | หารด้วยศูนย์ |
| `ได้ 0 ทั้งที่เห็นข้อมูล` | กำลังนับจากคอลัมน์ดิบ ให้เปลี่ยนไปใช้คอลัมน์ที่ทำความสะอาดแล้ว |

---

ใบสูตรนี้ครอบคลุมเฉพาะฟังก์ชันที่ปฏิบัติการนี้ใช้ รายละเอียดของอาร์กิวเมนต์ทุกตัวอยู่ใน `10_เอกสารเสริม/เอกสารเสริม_อาร์กิวเมนต์ของทุกฟังก์ชัน.docx`
